\
        <Window x:Class="RossvyazClient.MainWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                Title="АПК РОССВЯЗЬ" Height="750" Width="1250"
                WindowStyle="None" AllowsTransparency="True" Background="Transparent"
                KeyDown="MainWindow_KeyDown" KeyUp="MainWindow_KeyUp"
                WindowStartupLocation="CenterScreen">

            <Window.Resources>
                <Storyboard x:Key="OnAirPulse" RepeatBehavior="Forever">
                    <DoubleAnimation Storyboard.TargetName="StatusBadge" Storyboard.TargetProperty="Opacity"
                                     From="1.0" To="0.4" Duration="0:0:0.8" AutoReverse="True"/>
                </Storyboard>

                <Style x:Key="WindowCtrlBtn" TargetType="Button">
                    <Setter Property="Foreground" Value="#5C6066"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Width" Value="45"/>
                    <Setter Property="Height" Value="32"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#22FFFFFF"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Window.Resources>

            <Grid Margin="10">
                <Border CornerRadius="20" Background="#121417" BorderBrush="#2D3136" BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="20" Opacity="0.4" ShadowDepth="2"/>
                    </Border.Effect>
                </Border>

                <Border CornerRadius="20" Background="#121417" ClipToBounds="True">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="75"/>
                            <ColumnDefinition Width="260"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="260"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Grid.ColumnSpan="4" Background="#0F1113" MouseLeftButtonDown="OnHeaderMouseDown">
                            <Grid>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                                    <Ellipse Width="8" Height="8" Fill="#E63946" Margin="0,0,10,0"/>
                                    <TextBlock Text="АПК РОССВЯЗЬ v2.0 // КРИПТОКАНАЛ" Foreground="#5C6066" FontSize="10" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="—" Style="{StaticResource WindowCtrlBtn}" Click="Minimize_Click"/>
                                    <Button Content="✕" Style="{StaticResource WindowCtrlBtn}" Click="Close_Click"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Grid.Row="1" Grid.Column="0" Background="#0A0B0D">
                            <StackPanel Margin="0,20">
                                <Border Width="50" Height="50" Background="#E63946" CornerRadius="15" Margin="0,0,0,20">
                                    <TextBlock Text="РФ" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="1" Grid.Column="1" Background="#141619" BorderBrush="#1F2226" BorderThickness="0,0,1,0">
                            <DockPanel Margin="20">
                                <Grid DockPanel.Dock="Top" Margin="0,0,0,20">
                                    <TextBlock Text="ЛИНИИ СВЯЗИ" Foreground="#5C6066" FontSize="10" FontWeight="Bold"/>
                                    <Button Content="+" HorizontalAlignment="Right" Foreground="#4ADE80" Background="Transparent" BorderThickness="0" FontWeight="Bold" Click="OpenCreateChannelModal" Cursor="Hand"/>
                                </Grid>
                                <ScrollViewer VerticalScrollBarVisibility="Hidden">
                                    <ItemsControl x:Name="ChannelsList">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Button Margin="0,0,0,8" Padding="15,12" Background="#1F2226" BorderThickness="0" Click="Channel_Click" Tag="{Binding}">
                                                    <Button.Resources>
                                                        <Style TargetType="Border"><Setter Property="CornerRadius" Value="10"/></Style>
                                                    </Button.Resources>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="🔊" Margin="0,0,10,0" Opacity="0.5"/>
                                                        <TextBlock Text="{Binding}" Foreground="White" FontSize="13" FontWeight="SemiBold"/>
                                                    </StackPanel>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </DockPanel>
                        </Border>

                        <Grid Grid.Row="1" Grid.Column="2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="80"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="90"/>
                            </Grid.RowDefinitions>

                            <Border BorderBrush="#1F2226" BorderThickness="0,0,0,1">
                                <Grid Margin="30,0">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock x:Name="CurrentChannelTitle" Text="ШТАБНОЙ КАНАЛ" Foreground="White" FontSize="20" FontWeight="ExtraBold"/>
                                        <TextBlock Text="ENCRYPTION: AES-256 BIT" Foreground="#4ADE80" FontSize="9" Opacity="0.5"/>
                                    </StackPanel>
                                    <Border x:Name="StatusBadge" HorizontalAlignment="Right" VerticalAlignment="Center" Background="#1AE63946" CornerRadius="10" Padding="15,8">
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse x:Name="StatusDot" Width="8" Height="8" Fill="#E63946" Margin="0,0,10,0"/>
                                            <TextBlock x:Name="StatusLabel" Text="ОЖИДАНИЕ" Foreground="#E63946" FontSize="11" FontWeight="Black"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>

                            <ScrollViewer Grid.Row="1" x:Name="ChatScroll" Padding="30">
                                <StackPanel x:Name="ChatPanel"/>
                            </ScrollViewer>

                            <Grid Grid.Row="2" Margin="30,0,30,30">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="140"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="MessageInput" Background="#1A1C1F" Foreground="White" BorderBrush="#2D3136" BorderThickness="1" Padding="20,0" VerticalContentAlignment="Center" FontSize="14" KeyDown="MessageInput_KeyDown">
                                    <TextBox.Resources>
                                        <Style TargetType="Border"><Setter Property="CornerRadius" Value="12"/></Style>
                                    </TextBox.Resources>
                                </TextBox>
                                <Button Grid.Column="1" Content="ОТПРАВИТЬ" Margin="15,0,0,0" Background="#E63946" Foreground="White" FontWeight="Bold" Click="Send_Click">
                                    <Button.Resources>
                                        <Style TargetType="Border"><Setter Property="CornerRadius" Value="12"/></Style>
                                    </Button.Resources>
                                </Button>
                            </Grid>
                        </Grid>

                        <Border Grid.Row="1" Grid.Column="3" Background="#141619" BorderBrush="#1F2226" BorderThickness="1,0,0,0">
                            <StackPanel Margin="25">
                                <TextBlock Text="ЛИЧНЫЙ СОСТАВ" Foreground="#5C6066" FontSize="10" FontWeight="Bold" Margin="0,0,0,25"/>
                                <Button Content="ПРИГЛАСИТЬ" Background="#1A1C1F" Foreground="#4ADE80" BorderBrush="#4ADE80" BorderThickness="1" Padding="10" Margin="0,0,0,20" Click="Invite_Click">
                                    <Button.Resources>
                                        <Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style>
                                    </Button.Resources>
                                </Button>
                                <StackPanel x:Name="UsersPanel">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                        <Ellipse Width="10" Height="10" Fill="#4ADE80" Margin="0,0,12,0"/>
                                        <TextBlock Text="Командир (Вы)" Foreground="White" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Grid x:Name="CreateChannelModal" Grid.RowSpan="2" Grid.ColumnSpan="4" Visibility="Collapsed">
                            <Border Background="#CC000000"/>
                            <Border Background="#1A1C1F" Width="350" Height="220" VerticalAlignment="Center" CornerRadius="20" BorderBrush="#3D4147" BorderThickness="1">
                                <StackPanel Margin="30">
                                    <TextBlock Text="НОВЫЙ КАНАЛ" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
                                    <TextBox x:Name="NewChannelNameInput" Height="45" Background="#121417" Foreground="White" BorderBrush="#E63946" BorderThickness="1" Padding="10,0" VerticalContentAlignment="Center" Margin="0,0,0,20">
                                        <TextBox.Resources>
                                            <Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style>
                                        </TextBox.Resources>
                                    </TextBox>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Button Content="ОТМЕНА" Background="Transparent" Foreground="#5C6066" Click="CloseCreateChannelModal"/>
                                        <Button Grid.Column="1" Content="СОЗДАТЬ" Background="#E63946" Foreground="White" FontWeight="Bold" Height="35" Click="ConfirmCreateChannel">
                                            <Button.Resources>
                                                <Style TargetType="Border"><Setter Property="CornerRadius" Value="8"/></Style>
                                            </Button.Resources>
                                        </Button>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Window>
